using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Zephyr;

public class Main : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Model model = new ZephyrModel("test");
    }

    // Update is called once per frame
    void Update()
    {
    }
}
